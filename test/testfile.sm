input port one, one, first input
input port two, two, second input
output port one, one, first output
# comment: short names for inputs and outputs
# don't have to be same or different
output port two, two, second output

song To Each His Own

# indentation doesn't matter
patch Vanilla Through, Filter Two's Sustain
  conn one, all, one, all
  conn two, all, two, all
    bank 3, 2
    pc 12
    filter 64

patch One Up One Octave and CC Vol -> Pan, Two Down One Octave
  conn one, all, one, all
    xpose 12
    map 7, 10
  conn two, all, two, all
    xpose -12

song Another Song
notes
this song has note text
that spans multiple lines
end_notes

patch Two Inputs Merging
  conn one, 3, two, 4
    xpose 12
    zone 0, 63
  conn two, 1, two, 5
    zone 64, 127
    xpose -12

patch Split Into Two Outputs
  conn one, 3, one, 4
    xpose 12
    zone 0, 63
  conn one, 1, two, 5
    zone 64, 127
    xpose -12

set Song List One
To Each His Own
Another Song
end

set Song List Two
Another Song
To Each His Own
end
